#include <stdio.h>

int main() {
    int a;
    scanf("%d",&a);
    if(a==0){
        printf("num is zer0");
    }
    else
    {
    if(a>0)
    {
        printf("num is positive");
    }
   else
    {
        printf("num is negitive");
    }
   }
    
}
